server {
    listen 80;
    listen [::]:80;
    server_name neo4j-bolt-staging.alkanode.com;

    root /var/www/letsencrypt;

    location ^~ /.well-known/acme-challenge/ {
        default_type text/plain;
        try_files $uri =404;
    }

    location / {
        return 404;
    }
}
